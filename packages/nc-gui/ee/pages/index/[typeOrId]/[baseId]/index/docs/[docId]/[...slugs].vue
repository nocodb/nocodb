<!--
  Catch-all route that swallows the human-readable slug segment appended
  to doc URLs (e.g. /docs/abc123/my-page-title). The slug is cosmetic and
  used only for SEO-friendly URLs — the actual doc is resolved by [docId].vue
  using the `docId` param. This component renders nothing.
-->
<template><span /></template>
