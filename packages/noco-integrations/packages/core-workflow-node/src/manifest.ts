import { type IntegrationManifest } from '@noco-integrations/core';

export const manifest: IntegrationManifest = {
  title: 'Core Workflow Nodes',
  icon: 'workflow',
  version: '0.1.0',
  description: 'Core workflow nodes for building automations (triggers, flow control)',
};

export default manifest;
