{
  "name": "@noco-integrations/smtp-auth",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@noco-integrations/core": "workspace:*",
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.17",
    "@vitest/coverage-v8": "^4.0.18",
    "rimraf": "^5.0.10",
    "typescript": "^5.8.3",
    "vitest": "^4.0.18"
  }
}
