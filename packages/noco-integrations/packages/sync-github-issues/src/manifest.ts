import type { IntegrationManifest } from '@noco-integrations/core';

const manifest: IntegrationManifest = {
  title: 'GitHub Issues',
  icon: 'githubSolid',
  version: '0.1.0',
  description: 'Sync GitHub issues and pull requests from your repositories',
};

export default manifest;
