const AGG_DEFAULT_COLS = {
  count: `\t\tcount: Int,\r\n`,
  avg: `\t\tavg: Float,\r\n`,
  min: `\t\tmin: Float,\r\n`,
  max: `\t\tmax: Int,\r\n`,
  sum: `\t\tsum: Float\r\n`,
};

const GROUPBY_DEFAULT_COLS = {
  count: `\t\tcount: Int,\r\n`,
};

export { AGG_DEFAULT_COLS, GROUPBY_DEFAULT_COLS };
